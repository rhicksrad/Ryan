{
  "name": "ryan-3d-world",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:ci": "vite --port 5173 --strictPort --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test",
    "verify:screenshot": "node scripts/verify-screenshot.mjs",
    "check:size": "node scripts/size-budget.mjs",
    "test:ci": "npm run test:e2e && npm run verify:screenshot",
    "ci": "npm run test:ci && npm run build && npm run check:size"
  },
  "dependencies": {
    "three": "^0.162.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.30",
    "@types/three": "^0.162.0",
    "playwright": "^1.42.1",
    "typescript": "^5.4.2",
    "vite": "^5.1.5"
  }
}
